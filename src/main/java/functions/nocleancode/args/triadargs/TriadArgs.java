package src.main.java.functions.nocleancode.args.triadargs;

import java.util.stream.Stream;

/*For example, consider the common overload of assertEquals that takes three arguments: assertEquals( message, expected, actual).
How many times have you read the message and thought it was the expected? I have stumbled and paused over that particular triad many times.
 In fact, every time I see it, I do a double-take and then learn to ignore the message./
 */

public class TriadArgs {

    void assertEquals(message, expected, actual) {


    }


}
